## Minimundo - Scripts SQL (versão enxuta)

Arquivos: schema.sql, inserts.sql, queries.sql, updates_deletes.sql

Como rodar (MySQL Workbench):
1. Abrir `schema.sql` e executar para criar o schema.
2. Executar `inserts.sql` para inserir dados de exemplo.
3. Testar consultas em `queries.sql`.
4. Testar manipulações em `updates_deletes.sql`.

Observações:
- `contrato_monstro` implementa N:N com chave composta.
